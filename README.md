# RAG-Powered-Domain-Expert
Document-grounded Q&amp;A system with sentence-aware chunking, pgvector HNSW semantic search, and threshold-based refusal that prevents the model from answering when the corpus doesn't cover the question — every claim carries an inline citation.
